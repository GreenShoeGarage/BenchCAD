# BENCHCAD v0.36.0 Complete Package

This archive combines the latest prebuilt BENCHCAD v0.36.0 static application with the proper GitHub repository README and its local screenshots.

## Deployable runtime

Copy the archive contents, including `index.html`, `sw.js`, `manifest.webmanifest`, and `assets/`, directly into a static web directory. The runtime works at a domain root or subdirectory such as `/projects/benchcad/`.

## Documentation

- `README.md` — GitHub repository front page and complete user/deployment overview.
- `RELEASE-README.md` — detailed v0.36.0 release-specific documentation retained from the application package.
- `RELEASE-NOTES.md` — release history.
- `KNOWN-LIMITATIONS.md` — current technical and product limitations.
- `docs/images/` — local screenshots referenced by the GitHub README.

## Validation evidence

The included JSON and text reports document the v0.36.0 UI/UX, geometry-worker, drawing-regression, Batch 28D, and static-package checks.

`SHA256SUMS.txt` covers every file inside this extracted package except the checksum list itself.
